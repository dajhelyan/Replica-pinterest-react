import React from 'react';

export const ContainerImg = ({ children }) => {

    return (
        <section>
            <div className="card-columns">
                {children}
            </div>
        </section>
    )

}